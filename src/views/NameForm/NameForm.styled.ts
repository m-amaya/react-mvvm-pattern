import styled from 'styled-components';

export const RootStyled = styled.form`
  align-items: center;
  display: flex;
  gap: 16px;
  justify-content: center;
`;

export const FieldsetStyled = styled.fieldset`
  align-items: center;
  border: none;
  display: flex;
  gap: 8px;
  margin: 0;
  padding: 0;
`;

export const LabelStyled = styled.label`
  display: flex;
  font-weight: bold;
`;
